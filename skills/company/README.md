# company skills

Skills synced only to boxes that hold the **company** profile (Primary, Adjacent, or LT). They land in `profiles/company/skills/` and load only when that profile is active — so they never bloat other functions.
