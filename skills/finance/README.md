# finance skills

Skills synced only to boxes that hold the **finance** profile (Primary, Adjacent, or LT). They land in `profiles/finance/skills/` and load only when that profile is active — so they never bloat other functions.
