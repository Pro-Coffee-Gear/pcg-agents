# marketing skills

Skills synced only to boxes that hold the **marketing** profile (Primary, Adjacent, or LT). They land in `profiles/marketing/skills/` and load only when that profile is active — so they never bloat other functions.
