# cs skills

Skills synced only to boxes that hold the **cs** profile (Primary, Adjacent, or LT). They land in `profiles/cs/skills/` and load only when that profile is active — so they never bloat other functions.
