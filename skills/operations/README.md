# operations skills

Skills synced only to boxes that hold the **operations** profile (Primary, Adjacent, or LT). They land in `profiles/operations/skills/` and load only when that profile is active — so they never bloat other functions.
