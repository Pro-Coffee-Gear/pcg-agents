# lt skills

Skills synced only to boxes that hold the **lt** profile (Primary, Adjacent, or LT). They land in `profiles/lt/skills/` and load only when that profile is active — so they never bloat other functions.
