# product skills

Skills synced only to boxes that hold the **product** profile (Primary, Adjacent, or LT). They land in `profiles/product/skills/` and load only when that profile is active — so they never bloat other functions.
