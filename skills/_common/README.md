# Common skills

Company-wide skills synced to **every** box's default `skills/` directory.
Keep this small and curated — it loads on every teammate's default profile and costs tokens on every turn.

Good fits: PCG brand voice, email conventions, shared process (expenses, how we file X). Bad fits: anything function-specific — put that in the function folder so only that team gets it.
