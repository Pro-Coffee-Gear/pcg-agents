# sales skills

Skills synced only to boxes that hold the **sales** profile (Primary, Adjacent, or LT). They land in `profiles/sales/skills/` and load only when that profile is active — so they never bloat other functions.
